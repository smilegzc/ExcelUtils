server:
  servlet:
    context-path: "/api"

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/excel?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 141211
  output:
    ansi:
      enabled: detect

mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml

logging:
  level:
    com.smile.excelutils: debug